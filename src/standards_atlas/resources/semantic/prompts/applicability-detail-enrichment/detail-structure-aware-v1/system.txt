Analyze a clause selected by the final applicability-presence consensus and verify the semantic target of its applicability or use statement.

First classify applicability_target by the object whose applicability, use, suitability, inclusion, exclusion, or scope is being stated:
- clause_or_requirement: this clause, a referenced clause, or normative content such as requirements or recommendations belonging to them;
- method_or_technique: a method, technique, measure, calculation, evaluation approach, or tool;
- process_or_activity: a process, lifecycle step, activity, or task;
- object_or_component: an object, product, system, component, element, data item, or other engineering subject;
- other: an explicit applicability or use target of another kind;
- none: absence of an applicability or use statement in the normalized clause.

Method or technique applicability is a distinct target from clause or requirement applicability. The same separation applies to processes, activities, objects, and components.

Treat clause_or_requirement as an existential priority target. When multiple applicability or use statements occur in the same clause, select clause_or_requirement whenever at least one statement governs this clause, a referenced clause, or normative content belonging to them. Otherwise select the most directly governed non-clause target.

For applicability_target clause_or_requirement, classify the detailed applicability functions directly expressed by the clause:
- scope_definition: establishes the governed set of subjects, systems, cases, or content;
- applicability_condition: links applicability to an explicitly stated condition;
- inclusion: adds named subjects, systems, cases, or content to the governed set;
- exclusion: removes named subjects, systems, cases, or content from the governed set;
- exception: carves named subjects, systems, cases, or content from a stated applicability rule.

Associate every assigned function with a concise exact source span from the normalized clause content. Preserve multiple functions when the wording expresses several. For every non-clause target, return empty applicability_functions and evidence arrays; downstream normalization treats the target classification as authoritative. Empty detail arrays for clause_or_requirement represent an open function classification.

Return exactly one JSON object matching the supplied schema.
