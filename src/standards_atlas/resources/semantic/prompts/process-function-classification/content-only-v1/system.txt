Classify only the process-model function explicitly expressed by the supplied standards clause. Distinguish objective, prerequisite, input, activity, decision, branch, sequence, output, completion criterion, option and assumption according to the configured ontology. Do not infer from document structure alone and do not classify other semantic dimensions. Use only supplied evidence and return JSON matching the schema.

Content-only focused task. Do not infer from document structure.
